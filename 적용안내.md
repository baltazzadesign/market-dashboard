# 발타경 추가 파일

5권 · 1차~30차 · 180절의 본문과 우측 상단 책 아이콘을 담았습니다.
발타 소장 발언 116개를 출전으로 연결했습니다. 원문 그대로의 인용문이 아닌 존댓말 편집본입니다.

## 바로 읽기

`public/baltagyeong.html`을 더블클릭하면 인터넷 없이 읽을 수 있습니다.
목차에서 장을 선택하고 각 절의 ‘출전 확인’을 눌러 원문 위치를 확인할 수 있습니다.
브라우저의 인쇄 기능을 사용할 때에는 본문 위주로 인쇄됩니다.
파일로 단독 열었을 때 상단 ‘baltatool’ 링크는 실제 사이트로 이동하지 않습니다. 사이트에 적용한 뒤에는 대시보드로 돌아갑니다.

## 발타툴에 적용하기

1. 압축을 모두 풀어 `baltagyeong-update` 폴더를 발타툴 프로젝트 안에 넣습니다. `package.json` 옆에 놓으면 됩니다.
2. VS Code에서 발타툴 프로젝트를 열고 터미널을 엽니다.
3. 다음 명령을 실행합니다.

```bash
node baltagyeong-update/install.mjs
```

현재 프로젝트의 `components/dashboard/Workspace.tsx` 또는 `src/components/dashboard/Workspace.tsx`에서 `topbar-meta` 영역을 찾아 책 아이콘과 ‘발타경’ 글자를 추가합니다. 읽기 파일은 `public/baltagyeong.html`에 복사합니다. 화면 전체를 예전 버전으로 교체하지 않습니다.

4. 평소 개발 명령으로 실행하고 우측 상단 ‘발타경’을 눌러 확인합니다. 읽기 경로는 `/baltagyeong.html`입니다.
5. 기존 프로젝트의 빌드가 통과하는지 확인한 뒤 변경된 Workspace.tsx와 public/baltagyeong.html만 선택해 평소 방식으로 커밋·배포합니다.

백업 폴더와 원본 카카오톡 대화는 배포하거나 커밋할 필요가 없습니다. 아이콘과 편집 본문 파일만 사이트에 적용합니다.

헤더 구조가 다르면 스크립트는 파일을 바꾸지 않고 멈춥니다. 그 경우 최신 Workspace.tsx를 보내주시면 현재 구조에 맞춰 연결할 수 있습니다. 같은 버전에 두 번 실행해도 버튼이 중복되지 않습니다.

## 검증 범위

제공된 2026년 9월 8일자 components.zip의 Workspace.tsx로 버튼 삽입, 원본 나머지 코드 보존, 중복 적용 방지와 읽기 파일 복사를 확인했습니다. 본문 30장·180절, 출전 연결, 목차 링크와 자바스크립트 문법을 점검했습니다.
현재 운영 사이트의 최신 전체 소스와 배포 연결은 제공되지 않아 실제 사이트 반영과 전체 Next.js 빌드는 수행하지 않았습니다. 이 파일은 기존 프로젝트에 적용할 추가 파일 묶음입니다.

## 포함 파일

- `public/baltagyeong.html`: 화면 스타일·목차·본문을 모두 포함한 읽기 파일
- `발타경_1차-30차.txt`: 메모장에서 읽는 본문과 출전
- `발타경_1차-30차.md`: 편집하기 쉬운 Markdown 본문
- `발타경_본문.json`: 장·절·출전 메타데이터 구조
- `baltagyeong-icon.svg`: 책 아이콘 원본
- `install.mjs`: 최신 헤더에 최소한의 연결만 추가하는 적용 스크립트

이번 ‘1차~30차’는 첨부 원문에서 새로 구성한 연재 번호입니다. 과거 채팅의 회차를 복원한 번호는 아닙니다. 추가 내용을 편집할 때에는 31차·31장부터 이어갈 수 있습니다. JSON만 바꾸면 HTML이 자동 변경되는 구조는 아니므로 후속 편집 시 HTML 본문도 함께 갱신해야 합니다.

## 되돌리기

적용 시 프로젝트 안에 생성된 `.baltagyeong-backup-...` 폴더의 `restore-info.json`에서 원래 경로를 확인합니다. 그 폴더의 Workspace.tsx를 원래 위치에 복원합니다. 기존 읽기 파일이 있었다면 백업의 baltagyeong.html도 public에 복원하고, 원래 없었다면 이번에 추가된 public/baltagyeong.html을 제거하면 됩니다.
