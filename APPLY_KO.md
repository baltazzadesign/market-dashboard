# baltatool 차콜 테마 · 차트 업그레이드

기존 market-dashboard에 적용하는 패치입니다. 전체 프로젝트가 아닙니다.
2026-09-08 전달한 이전 디자인 패치의 변경도 포함합니다.

## 적용 순서

1. 개발 서버가 실행 중이면 터미널에서 Ctrl+C로 종료하세요.
2. 기존 프로젝트의 아래 4개 파일을 프로젝트 밖의 별도 폴더에 백업하세요.
   app/globals.css
   components/dashboard/Insights.tsx
   components/dashboard/MarketCharts.tsx
   components/dashboard/Workspace.tsx
3. ZIP을 별도 폴더에 압축 해제하세요.
4. ZIP 안의 코드 파일 5개를 기존 프로젝트의 같은 경로에 복사하세요.
   기존 4개 파일은 교체하고, chart-model.ts는 새로 추가합니다.
   app 또는 components 폴더 전체를 삭제하지 마세요.
5. 기존 프로젝트 폴더에서 아래를 실행하세요.

   npm run build

6. 빌드가 성공하면, 로컬 확인은 npm run dev 실행 후 http://localhost:3000 에서 합니다.
   확인을 마치고 개발 서버를 종료한 뒤 아래 명령을 한 줄씩 실행하세요.

   git add app/globals.css components/dashboard/Insights.tsx components/dashboard/MarketCharts.tsx components/dashboard/Workspace.tsx components/dashboard/chart-model.ts
   git commit -m "Upgrade dark theme and interactive charts"
   git push

추가 패키지 설치 및 환경 변수 변경은 필요하지 않습니다.
기존 Git 연동 배포를 사용 중이라면 배포 결과를 확인한 뒤 운영 사이트를 새로고침하세요.
이번 전달 작업에서 운영 사이트 배포를 직접 실행하지는 않았습니다.

## 포함 파일

- app/globals.css — 블랙·차콜 화면 및 차트/모바일 스타일
- components/dashboard/Insights.tsx — 지표 카드·수급 패널 색상
- components/dashboard/MarketCharts.tsx — 차트 수치 표시·줌·이동·탐색 막대
- components/dashboard/Workspace.tsx — 참고 이미지 기반 차트 배치·시간축/커서 연동
- components/dashboard/chart-model.ts — 새 파일. 공통 색상·시리즈·시간 범위 계산

chart-model.ts가 누락되면 실행되지 않으므로 반드시 함께 복사하세요.

## 색상 및 배치

외인: 파랑 #5294ff
기관: 빨강 #ff545f
개인: 노랑 #f2ce53

시장 폭: 노랑 / 상승 비율: 빨강 / 하락 비율: 파랑
KOSPI: 초록 / KOSDAQ: 보라

상단에 시장 폭·상승/하락 비율, 그 아래 넓은 주 차트(기본: 투자자 수급),
하단에 KOSPI·KOSDAQ 개별 차트를 배치했습니다.
일별 분석에는 시장점수·가속도도 함께 표시합니다.
주 차트의 탭을 바꾸면 같은 지표의 중복 비교 차트는 숨깁니다.
이전 설정에서 비교 차트를 껐다면 화면 설정에서 비교 차트를 켜세요.

## 차트 사용법

- 상단 값: 최신으로 수신한 기록의 수치와 기준 시각. 커서를 움직여도 최신값 유지.
- 커서 줄: 마우스가 가리키는 시각의 값. 기록이 없으면 없다고 표시.
- 외인/기관/개인 등 상단 항목 클릭: 해당 그래프 표시·숨김.
- + / −: 시간축 확대·축소. 최소 5분 구간까지 확대.
- Ctrl 또는 Command를 누른 채 휠: 커서 위치 중심 시간축 줌.
- 일반 휠: 페이지 스크롤 유지.
- 드래그: 기본은 구간 확대. '드래그: 확대' 버튼을 누르면 이동 모드 전환.
- 왼쪽/오른쪽 버튼: 보이는 구간 길이의 약 1/4만큼 시간대 이동.
- 하단 탐색 막대: 양끝 핸들로 범위 변경, 선택 영역 드래그로 이동.
- 시작/끝 슬라이더: 터치 및 키보드로도 시간 범위 조절.
- 더블클릭 또는 전체: 정규장 09:00~15:30 구간 복귀.
- 최근 1시간: 최신 수신 시각에 맞춰 1시간 범위를 따라감.
- 확대 아이콘: 해당 지표를 큰 차트 창으로 열기. Esc로 닫기.
- 차트 바깥 테두리에 키보드 초점을 둔 상태에서 +/−, 좌우 화살표, Home 사용 가능.
- 차트 간 시간 범위와 커서 시각 연동.

장 시작/종료 경계에서는 시간 범위가 정규장 안으로 제한됩니다.
시장 폭·시장점수의 점 마커는 강한 신호만 표시하며,
전체 신호는 기존 기록 표에서 계속 확인할 수 있습니다.

## 실시간 표시의 기준

기존 데이터 수집·자동 갱신은 60초 주기입니다. 초 단위 틱 스트리밍은 추가하지 않았습니다.
새 기록을 받을 때 상단 수치가 갱신됩니다.
과거 날짜, 자동 갱신 일시정지, 지연 및 수급 데이터 상태를 구분해 표시합니다.
마우스로 과거 시각을 탐색해도 상단 최신값과 커서값을 구분합니다.
줌으로 최신 시각이 화면 밖에 있어도 상단은 최신 수신 기록 기준입니다.
기존 API·인증·DB·수급 및 신호 계산은 변경하지 않았습니다.

## 검증 결과와 범위

- Next.js 16.2.4 프로덕션 빌드 성공
- TypeScript 검사 통과
- 확대 중심점, 최소 5분, 장 경계, 좌우 이동 검증
- 280개 생성 구간의 확대 범위·정수 분 경계 검증
- 누락 구간, 0과 null 구분, 비율 단위 변환, 투자자별 색상 검증
- package.json/package-lock.json/설정/API/시장 계산 파일 원본 유지 확인
- 실제 API 연결, 브라우저의 휠·드래그·터치·시각 레이아웃은 실기 검증하지 않음

## 적용 후 빠른 확인

1. 대시보드와 일별 분석에서 수급선이 파랑/빨강/노랑인지 확인하세요.
2. 상단 최신 수치가 마우스 이동 중에도 유지되는지 확인하세요.
3. +/−, 구간 드래그, 이동 모드, 휠, 탐색 막대, 큰 창을 확인하세요.
4. 전체 버튼으로 되돌리고 최근 1시간을 눌러 최신 구간을 확인하세요.
5. 연결 상태가 정상인 장중에는 새 기록 수신 뒤 기준 시각이 갱신되는지 확인하세요.

## 복구

개발 서버를 종료하고 백업한 기존 4개 파일을 원래 위치로 복사해 되돌리세요.
이번에 새로 추가한 chart-model.ts는 다른 곳에서 사용하지 않는 경우 삭제해도 됩니다.
